# PayPal checkout setup and acceptance

Status: **Sandbox server capture verified for both SKUs in real Chrome** on
23 September 2026. The credit order captured 39 NOK and granted exactly 100 test
credits; replaying its return kept one grant. The 29 NOK digital order delivered
the real JPG and TXT, whose downloaded sizes/SHA-256 match the stored assets.
Logged-out requests to both signed links return 401. No real money was spent.

Live credentials and the approved Live webhook were saved, but the new release
is only deployed to isolated Preview and Live acceptance is not yet verified. Do not describe the
whole payment integration as production-ready until the remaining checks pass.

## Credentials and environments

Existing REST apps in the owner's PayPal Developer dashboard are reused:

| PayPal app | Destination | Credentials |
| --- | --- | --- |
| Default Application (Sandbox) | Local `http://localhost:3000`; Vercel Preview credentials saved | Sandbox only |
| veggastare (Live) | Vercel project `dev-veggastare`, Production only | Live only |

Production needs `PAYPAL_CLIENT_ID`, `PAYPAL_CLIENT_SECRET`, and
`PAYPAL_WEBHOOK_ID`; the Live readiness check requires all three. Local Sandbox
server capture works with the two credential variables; external webhook
delivery additionally requires its own subscription ID. Keep secrets server-side: no `NEXT_PUBLIC_` prefix,
commits, screenshots, chat messages, logs or browser storage. Do not copy Live
credentials into localhost or Preview. Redeploy after changing Vercel variables.

`paypalEnvironment()` allows Live only when `VERCEL=1` and
`VERCEL_ENV=production`; a local production build still selects Sandbox.
The showcase path does not use the legacy `PAYPAL_API_URL` override.

In each REST app's webhook settings, register the matching environment's
HTTPS callback and subscribe to **PAYMENT.CAPTURE.COMPLETED**,
**PAYMENT.CAPTURE.REFUNDED**, and **PAYMENT.CAPTURE.REVERSED**:

- Production: `https://www.veggat.com/api/webhooks/paypal`.
- Sandbox: the dedicated Preview's `/api/webhooks/paypal` URL, reachable by
  PayPal without a preview password. Do not point Sandbox events at Production.
- Copy that subscription's ID into `PAYPAL_WEBHOOK_ID`; it is not the REST app
  ID or client ID. Localhost requires a separately approved HTTPS forwarding
  arrangement for webhook delivery; server capture can be tested locally first.

The approved Live subscription was saved successfully (ID `50807169JH083430L`)
with exactly these three events. Vercel Production holds all three Live values
as secret variables; Development and Preview hold separate scoped copies of the
two Sandbox credential values. The Sandbox subscription `4D346436GJ973660K`
now targets the isolated showcase branch alias with exactly the same three
capture events. It was created and read back through PayPal's official Sandbox
Webhooks API; no existing subscription was modified. `PAYPAL_WEBHOOK_ID` was
saved as a sensitive Preview-only variable scoped to `showcase/ai-revival`.
The first CLI redeploy did not load the branch-scoped runtime webhook value
(the listener still returned 503). A fresh Preview candidate explicitly binds
the same subscription ID and canonical AUTH_URL at build/runtime. The stable
Preview now returns 200 for health and 401 `INVALID_SIGNATURE` for an unsigned
capture event. Actual PayPal delivery/refund/replay must still be verified before
calling this ready; an unsigned probe is not evidence of accepted PayPal delivery.
No keys were rotated and no Live keys were put on localhost. The ignored
`.private-showcase/paypal-development.env` is generated by `vercel env pull`;
`node scripts/start-paypal-sandbox.mjs --dist-dir=<isolated-built-directory>`
verifies Sandbox authentication and injects only its PayPal values into :3000.

### Isolated Preview database

On 23 September 2026, the owner-authorized Neon branch `veggat-paypal-sandbox`
was created in `VeggaStareMain` using **schema only** from `MainLive`. It remains
on the existing Free plan, with 0.25 CU fixed compute, five-minute scale-to-zero,
and no automatic expiration. Existing branches and production settings were not
changed. `DATABASE_URL_MAINPREVIEW` is a Vercel **Preview-only Secret**.

All 115 application tables were checked empty before seeding. The copied schema
was compared against the source using read-only transactions (columns,
constraints, indexes, enums, views, functions, triggers and policies). All 45
migration checksums matched the repository, allowing equivalent LF/CRLF line
endings. Only verified migration metadata was baselined; Prisma `migrate status`
and `migrate deploy` then reported an up-to-date schema with no pending work.

`scripts/baseline-schema-only-preview.mjs` requires a separately verified target
hostname, refuses populated application tables or a source/target endpoint match,
and defaults to a dry run. Never replay the historical SQL into an already
cloned schema or replace the Preview URL with production to get a build through.
Both runtime and migration configuration now fail closed if Preview has no
isolated URL, or if its endpoint matches `DATABASE_URL_MAINLIVE`.

The branch now contains only synthetic QA data: a non-login seller, an ordinary
password buyer, one showcase company and the two reviewer SKUs. The private JPG
and TXT were provisioned under the synthetic seller's storage path; raw requests
return 403 and authenticated downloads match their SHA-256. No purchase, AI
credit grant, real user, real order or production session was copied. The QA
password and test connection are in ignored local setup files, never this guide.
The one-use loopback setup receiver has stopped.

Preview release `91f1fbe` is built and READY as
`dpl_9raz9RMLCkv8zzqaztb8xt3qKTc1`, using the isolated Neon endpoint. Its stable
alias is `dev-veggastare-git-showcase-ai-revival-v3ggas-projects.vercel.app`.
An `AUTH_URL` override is saved for Preview branch `showcase/ai-revival` only;
the second deployment includes that configuration change. Both Vercel builds
passed webpack, TypeScript, migrations and 187 static pages.

The owner confirmed a domain-only protection exception on 23 September 2026.
It is saved for the stable showcase alias only; project-wide Vercel authentication
stays enabled. Public health now returns 200/healthy. An unsigned webhook POST
reaches the app and returns 503 `WEBHOOK_NOT_CONFIGURED` on the old deployment,
not a grant. The configured-webhook redeploy and actual delivery tests remain.

Historical pre-exception observation: Vercel's team-login protection returned 401
before requests reached the app. That blocker is resolved by the approved
domain-only exception, not a project-wide protection or billing change.
Verify the app's auth origin/password login and actual delivery/refund/replay
after the configured redeployment. The provisioned database, subscription and
successful build are not proof that the complete Preview checkout works.

The current browser flow follows PayPal-hosted approval after a server-created
Orders v2 order. A JS v6 button is a separate frontend integration, not required
to replace the verified server create/capture path. If added, follow the
[SDK v6 configuration guide](https://docs.paypal.ai/developer/how-to/sdk/js/v6/configuration)
and keep the client secret on the server.

## Sandbox first

Use a Sandbox buyer, not a real card. Verify both reviewer SKUs separately and
then together: Interview Pack 29 NOK; 100 AI credits 39 NOK. Prices come from
`frontend/lib/showcase-catalog.ts`, never client-supplied totals.

1. Sign in with an isolated non-demo test account. Create a checkout, cancel
   at PayPal, and verify no goods or credits were granted.
2. Approve a new order and capture it through the server. Check amount,
   currency, merchant, order binding, capture ID and the buyer receipt.
3. Download the private JPG and TXT as the buyer; logged-out and unrelated
   accounts must be denied. Expired download links must be rejected.
4. Verify credits increased exactly once. Repeat the return/capture and resend
   the actual app webhook: no duplicate entitlement or balance increase.
5. Tampered totals, unsigned events and a return URL alone must not fulfill.
   Confirm the daily attempt cap remains enforced; do not reset it to loop tests.
6. Send one bounded credit-funded AI message, then prove zero-balance rejection
   without a provider call. Do not replenish credits merely to hide a failure.

The listener uses PayPal postback signature verification and preserves the
original event JSON. PayPal's simulator mock events do **not** support postback
verification; use events from an actual Sandbox app transaction. See
[PayPal webhook verification](https://developer.paypal.com/api/rest/webhooks/rest/).

## Live acceptance and refunds

After Sandbox passes, configure the separate Live app in Vercel Production.
The owner completes the authorized micro-purchases with their own buyer/card;
verify each receipt, download and credit grant as above. The free isolated demo
remains available, so reviewers never have to pay. Record only non-sensitive
evidence, not credentials or payment details.

For a refund, the owner locates the exact capture in PayPal's business Activity,
confirms the buyer and NOK amount, and uses the transaction's refund control.
Record its refund ID privately. The new reconciliation code verifies fresh
provider proof, uses the same fulfillment lock, reverses the purchase grant once,
revokes downloads and keeps partial refunds in payment review. Already-used
credits become a disclosed adjustment, not a negative spendable balance or a
card charge. A later purchase first offsets that adjustment. Unit/isolated
database tests cover concurrent/replayed/out-of-order reconciliation, but a real
Sandbox refund webhook has not yet been exercised end-to-end. That remains a
release gate; dashboard success alone is not entitlement-reconciliation proof.

Creating an order uses an idempotency key; fulfillment is separately guarded by
the verified capture ID. Provider idempotency does not replace database guards.
Reference: [PayPal request idempotency](https://developer.paypal.com/api/rest/reference/idempotency/).
