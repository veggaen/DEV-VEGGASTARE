// Hooks Export Index

// Poll Navigation Hook - URL sync, mouse navigation, shareable links
export { 
  usePollNavigation,
  type PollNavigationState,
  type UsePollNavigationOptions,
  type UsePollNavigationReturn,
} from "./use-poll-navigation";

// Re-export any existing hooks
// Add more hook exports here as needed
