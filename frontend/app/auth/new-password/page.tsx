import { MyNewPasswordForm } from "@/components/uicustom/auth/forms/new-password-form";

const MyPageNewPassword = () => {
    return (
      <MyNewPasswordForm />
    )
}
export default MyPageNewPassword; // this needs to be default because its in a (protected) route and is a page.tsx